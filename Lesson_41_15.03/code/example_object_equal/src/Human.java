public class Human {
    public int age; // public bedeutet offenes Feld der Klasse sagt über den Alter aus
    public boolean isWorker; // -//- arbeitet Human oder nicht

    public Human(int age, boolean isWorker) {
        this.age = age;
        this.isWorker = isWorker;
    }
}
